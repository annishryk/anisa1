import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'quiz_annisa_haryoko',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
